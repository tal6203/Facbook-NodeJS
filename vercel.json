{
  "version": 2,
  "builds": [
    { "src": "app.js", "use": "@now/node-server" }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "app.js" }
  ],
  "env": {
    "MONGODB_URI": "mongodb://root:rootpassword@localhost:27022/node-auth?authSource=admin",
    "NODE_ENV": "production"
  }
}
